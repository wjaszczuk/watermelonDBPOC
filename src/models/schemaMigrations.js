import {schemaMigrations as schemaMigrationsFn} from '@nozbe/watermelondb/Schema/migrations';

export const  schemaMigrations = schemaMigrationsFn({
  migrations: [
    // We'll add migration definitions here later
  ],
});
